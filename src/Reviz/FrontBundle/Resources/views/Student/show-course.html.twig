{% extends "RevizFrontBundle:Layouts:studentDash.html.twig" %}

{% block content %}

    <span><a href="{{ path('student_category_show', { 'id' : category.getId() }) }}">Retour</a></span>

    <h2>{{ course.title }}</h2>

    <p>{{ course.content }}</p>

{% endblock content %}