{% extends 'base.html.twig' %}

{% block body %}
    <h1>Level</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ level.id }}</td>
            </tr>
            <tr>
                <th>Reflevel</th>
                <td>{{ level.refLevel }}</td>
            </tr>
            <tr>
                <th>Nbmodule</th>
                <td>{{ level.nbModule }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_level_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_level_edit', { 'id': level.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
