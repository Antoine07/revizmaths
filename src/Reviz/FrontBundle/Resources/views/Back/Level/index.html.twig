{% extends 'base.html.twig' %}

{% block body %}
    <h1>Level list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
            </tr>
        </thead>
        <tbody>
        {% for level in levels %}
            <tr>
                <td><a href="{{ path('admin_level_show', { 'id': level.id }) }}">{{ level.id }}</a></td>
                <td><a href="{{ path('admin_level_edit', { 'id': level.id }) }}">{{ level.name }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_level_show', { 'id': level.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_level_edit', { 'id': level.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_level_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
