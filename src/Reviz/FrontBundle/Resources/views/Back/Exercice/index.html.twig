{% extends 'base.html.twig' %}

{% block body %}
    <h1>Exercice list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for exercice in exercices %}
            <tr>
                <td><a href="{{ path('admin_exercice_show', { 'id': exercice.id }) }}">{{ exercice.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_exercice_show', { 'id': exercice.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_exercice_edit', { 'id': exercice.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_exercice_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
