{% extends 'base.html.twig' %}

{% block body %}
    <h1>Method list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for method in methods %}
            <tr>
                <td><a href="{{ path('admin_method_show', { 'id': method.id }) }}">{{ method.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_method_show', { 'id': method.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_method_edit', { 'id': method.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_method_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
