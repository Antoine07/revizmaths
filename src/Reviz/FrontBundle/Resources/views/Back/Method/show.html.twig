{% extends 'base.html.twig' %}

{% block body %}
    <h1>Method</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ method.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_method_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_method_edit', { 'id': method.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
